# Stub server only (REST API lives in transpak-backend / PostgreSQL).
# Default port avoids clashing with transpak-backend (5000).
PORT=5001
CLIENT_ORIGIN=http://localhost:5173
TRANSPAK_BACKEND_URL=http://127.0.0.1:5000
