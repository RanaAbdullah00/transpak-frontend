# Cloudflare Pages — deploy: npm run build && npx wrangler pages deploy dist
name = "transpak-frontend"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# REQUIRED in Cloudflare Pages Dashboard → Settings → Environment variables (Production):
# VITE_API_URL = https://transpak-backend-1.onrender.com
# Optional: VITE_API_DEBUG=true (logs all API URLs in browser console)
