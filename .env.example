# Optional: full API base including /api (overrides dev proxy when set).
# VITE_API_URL=http://localhost:5000/api

# Dev proxy target when VITE_API_URL is empty (see vite.config.mts).
VITE_PROXY_TARGET=http://127.0.0.1:5000

