# Local dev: use Vite proxy to your backend (must match transpak-backend PORT).
# Comment out VITE_API_URL so requests go to /api → proxy below.
# VITE_API_URL=https://transpak-backend-1.onrender.com

VITE_PROXY_TARGET=http://127.0.0.1:10000
