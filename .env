# Local dev — Vite proxies /api and /socket.io to the backend.
# Match transpak-backend PORT in .env (default 10000). If backend picks another port, update this.
VITE_PROXY_TARGET=http://127.0.0.1:10000
