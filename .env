# Local dev: must match the port your backend actually listens on (see npm start log).
# Default backend local port is 5000 (transpak-backend/.env PORT=5000).
VITE_API_URL=https://transpak-backend-1.onrender.com/api/auth/signup
